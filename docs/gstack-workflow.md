# gstack Workflow Memory

## 2026-05-15 Ops Project Canvas

- `/ops/projects/:projectId` canvas is treated as an API-hydrated product surface, not a static mock.
- IA nodes should be derived from routes, screens, docs intent, and user actions. Architecture nodes should be derived from API routes, services, storage/schema, integrations, jobs, auth/security, deploy/config, and evidence files.
- Canvas persistence uses the same payload shape for local JSON fallback and Supabase `ops_project_canvases`; the frontend should not depend on the storage backend.
- Live GitHub analysis is currently served through Next API routes using authenticated `gh` CLI. A future NestJS app should preserve the existing route contracts and move the implementation behind the same DTO shape.
- A standalone NestJS scaffold now exists in `apps/ops-api`. It mirrors the Next API DTOs for canvas reads/saves/export and GitHub tree/content analysis, using shared `lib/ops/project-management-contract.mjs`.
- Next API routes can now proxy to NestJS when `OPS_API_BASE_URL` or `PORTFOLIO_OPS_API_BASE_URL` is set. Proxied responses include `x-ops-backend-source: nest`.
- NestJS now treats Supabase as the primary projects/tasks/canvas source when service-role env is configured, otherwise it reports and uses local JSON fallback. Repo canvas analysis uses live GitHub tree/content for non-Portfolio repos.
- Goal 1 of the Code-backed Planning Canvas roadmap is complete: `project-planning-contract.mjs` now creates Manyfast-style PRD, Requirement, Feature, Specification, IA pages, User Flow, Wireframe blocks, Architecture nodes, GitHub evidence, and agent tasks from repo analysis.
- Goal 2 of the Code-backed Planning Canvas roadmap is complete: `supabase/ops-schema.sql` now includes normalized planning tables and RLS for PRD/spec/IA/user-flow/wireframe/architecture/evidence/suggestions/exports. Browser access remains blocked; server service-role access is the intended path.
- Goal 3 of the Code-backed Planning Canvas roadmap is complete: NestJS now exposes planning document generate/read/save plus split IA/specification/architecture/evidence endpoints. It uses Supabase first when service-role env exists and `data/ops/project-planning.json` as local fallback otherwise.
- Goal 3 runtime smoke for `portfolio` produced 4 requirements, 3 features, 15 specifications, 8 IA pages, 1 user flow, 8 wireframes, 7 architecture nodes, and 182 GitHub evidence records. `PATCH /ops/projects/:id/planning` persisted successfully and later `GET` returned `source: saved-planning`.
- Supabase env status on 2026-05-16: Vercel project `chanes-projects-0d01bb7c/portfolio` is linked and `vercel env pull .env.local --yes` works, but it only provided `VERCEL_OIDC_TOKEN`; no `PORTFOLIO_SUPABASE_URL` or `PORTFOLIO_SUPABASE_SERVICE_ROLE_KEY` was available locally, in Vercel development env, GitHub Actions secrets, or shell env. NestJS now loads local env files automatically, but actual DB verification still requires those two Supabase values.
- Supabase DB status on 2026-05-17: copied the existing Portfolio Supabase env from `/Users/hyeon-yongchan/Desktop/Portfolio/.env` into current `.env.local`, registered both values in Vercel development/preview/production env, linked Supabase project `ftkhgqmjpnmrqexnmxcc` (`potfolio`), applied `supabase/ops-schema.sql`, and verified all ops + planning tables with `npm run ops:doctor`.
- Supabase-backed planning API smoke on 2026-05-17: `POST /ops/projects/portfolio/planning/generate` saved to Supabase and `GET /ops/projects/portfolio/planning` returned `source: saved-planning`. DB row counts for `portfolio`: planning document 1, PRD 1, requirements 4, features 3, specifications 15, IA pages 8, user flows 1, user flow steps 8, wireframe blocks 8, architecture nodes 7, GitHub evidence 184.
- Goal 4 of the Code-backed Planning Canvas roadmap is complete: Next BFF routes now proxy planning, planning generate, IA, specifications, architecture, user flows, wireframes, and evidence to NestJS. Runtime proof used Next `3014` with `OPS_API_BASE_URL=http://localhost:4013`; responses included `x-ops-backend-source: nest`.
- GitHub repo picker fix on 2026-05-17: `/ops/projects/new` now fetches `GET /api/ops/projects` on mount. That API uses live GitHub via `gh api user/repos` and falls back to cache. Runtime proof returned 39 repositories with `source: live-github`.
- Canvas layout fix on 2026-05-17: `applyReadableCanvasLayout()` separates IA, Architecture, and Export/AI into stable lanes; `ProjectCanvasPage` applies it when hydrating saved canvases and exposes an `Auto layout` action. The current `portfolio` canvas was saved back to Supabase with the lane layout.
- Goal 5 of the Code-backed Planning Canvas roadmap is complete: `ProjectCanvasPage` now has IA Tree, Spec Directory, User Flow, Wireframe Blocks, System Architecture, GitHub Evidence, Agent Tasks, Export, and Deploy modes. Each mode filters visible canvas nodes and shows a planning-backed detail panel from `GET /api/ops/projects/:id/planning`.
- Network model clarification: browser requests should normally hit Next on the frontend port, for example `localhost:3001/api/ops/projects`. Next is the auth/BFF layer. Routes with `x-ops-backend-source: nest` are proxied internally from Next to Nest via `OPS_API_BASE_URL`; routes without that header are handled directly by Next server code.
- Goal 6 of the Code-backed Planning Canvas roadmap is complete: `Generate planning from GitHub` now calls the Nest planning generate endpoint, saves normalized planning plus a generated planning canvas, and refreshes the frontend canvas immediately. Runtime proof on 2026-05-17: generated planning returned 8 IA pages, 7 architecture nodes, and 32 canvas nodes; subsequent canvas GET returned `source: saved-canvas` with `planning-root`.
- Goal 7 of the Code-backed Planning Canvas roadmap is complete: AI suggestions now have Nest + Next APIs for list/create/generate/approve/reject. Approval applies the proposed normalized planning change and regenerates/saves the canvas; rejection only changes suggestion status. Runtime proof on 2026-05-17 through Next `3020` -> Nest `4020`: approved `AI Approval Workflow`, planning architecture count became 8, canvas returned `x-ops-backend-source: nest`, 34 nodes, and the approved node was present.
- Goal 8 of the Code-backed Planning Canvas roadmap is complete: planning exports now generate PRD, Functional Spec, IA, User Flow mermaid, Architecture, Agent Brief, and Planning Canvas markdown from saved planning data. Runtime proof on 2026-05-17 through Next `3021` -> Nest `4021`: all `POST /api/ops/projects/portfolio/export/:type` calls returned `x-ops-backend-source: nest`, expected filenames, and content with project, repository, evidence, and architecture references.
- Verification commands known to pass for this slice: `node --test tests/ops-project-management-contract.test.mjs`, `npm run typecheck`, `npm run lint`, `npm run build`.
- NestJS verification command: `npm run ops-api:build`; runtime smoke uses `OPS_API_PORT=4010 npm run ops-api:start`.
